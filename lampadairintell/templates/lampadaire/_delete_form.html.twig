<form method="post" action="{{ path('app_lampadaire_delete', {'id': lampadaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mt-3">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lampadaire.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>