{# templates/hello_world/index.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}test Page lampadaire{% endblock %}

{% block body %}
    <h1>{{ message }}</h1>

    <a href="{{ path('app_lampadaire_index') }}" class="btn btn-primary">Go to Lampadaire</a>
    <a href="{{ path('app_quartier_index') }}" class="btn btn-secondary">Go to Quartier</a>

    <style>
        .btn {
            display: inline-block;
            margin: 10px 10px 0 0;
            padding: 10px 20px;
            font-size: 16px;
            text-decoration: none;
            color: #fff;
            border-radius: 5px;
        }
        .btn-primary {
            background-color: #007bff;
        }
        .btn-secondary {
            background-color: #6c757d;
        }
        .btn:hover {
            opacity: 0.9;
        }
    </style>
{% endblock %}
