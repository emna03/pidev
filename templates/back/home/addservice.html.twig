{% extends 'base_admin.html.twig' %}

{% block title %}Ajouter un Service d'intervention{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Ajouter un service d'intervention</h1>

    <div class="card p-4 shadow-sm rounded-3">
        {% include 'back/home/formservice.html.twig' with { 'form': form, 'button_label': 'Ajouter' } %}
    </div>

    {# Ajout du bouton pour aller à la liste des services #}
    <div class="mt-4 text-center">
        <a href="{{ path('list_service') }}">
            <button class="btn btn-primary">Voir la liste des services</button>
        </a>
    </div>
</div>
{% endblock %}
