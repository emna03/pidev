<form method="post" action="{{ path('app_tentative_fraude_delete', {'id': tentative_fraude.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tentative_fraude.id) }}">
    <button class="btn">Delete</button>
</form>
