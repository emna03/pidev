{# templates/poubelle_intelligente/_form.html.twig #}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-3">
        <i class="fas fa-save"></i> {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}