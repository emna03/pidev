{# templates/poubelle_intelligente/_delete_form.html.twig #}
<form method="post" action="{{ path('app_poubelle_intelligente_delete', {'id': poubelle_intelligente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poubelle_intelligente.id) }}">
    <button class="btn btn-danger btn-sm">
        <i class="fas fa-trash"></i> Delete
    </button>
</form>