<form method="post" action="{{ path('app_dossier_fiscale_delete', {'id': dossier_fiscale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier_fiscale.id) }}">
    <button class="btn">Delete</button>
</form>
