<!DOCTYPE html>

<title>DeclarationRevenus</title>

{% block body %}
    <h1>DeclarationRevenus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ declaration_revenu.id }}</td>
            </tr>
            <tr>
                <th>Montant_revenu</th>
                <td>{{ declaration_revenu.montantRevenu }}</td>
            </tr>
            <tr>
                <th>Source_revenu</th>
                <td>{{ declaration_revenu.sourceRevenu }}</td>
            </tr>
            <tr>
                <th>Date_declaration</th>
                <td>{{ declaration_revenu.dateDeclaration}}</td>
            </tr>
            <tr>
                <th>Preuve_revenu</th>
                <td>{{ declaration_revenu.preuveRevenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_declaration_revenus_index') }}">back to list</a>

    <a href="{{ path('app_declaration_revenus_edit', {'id': declaration_revenu.id}) }}">edit</a>

    {{ include('declaration_revenus/_delete_form.html.twig') }}
{% endblock %}
