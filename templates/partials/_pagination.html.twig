{% if pagination.getTotalItemCount > 0 %}
    <div class="pagination-container">
        <div class="pagination custom-pagination-style-1 pagination-rounded">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
{% endif %}
