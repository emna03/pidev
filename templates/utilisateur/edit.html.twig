{% extends 'base.html.twig' %}

{% block title %}Modifier Profil{% endblock %}

{% block stylesheets %}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
<style>
  body {
    background-color: #f8f9fa;
    font-family: 'Roboto', sans-serif;
  }

  .error-msg {
    color: red;
    font-size: 0.9rem;
    margin-top: 5px;
    font-weight: bold;
  }

  .profile-header {
    background: linear-gradient(135deg, #667eea, #764ba2);
    color: white;
    padding: 2rem;
    border-radius: 0.5rem 0.5rem 0 0;
    text-align: center;
  }

  .profile-header img {
    width: 120px;
    height: 120px;
    object-fit: cover;
    border-radius: 50%;
    border: 4px solid #fff;
    margin-bottom: 1rem;
  }

  .btn-primary {
    background-color: #764ba2;
    border-color: #764ba2;
  }

  .btn-primary:hover {
    background-color: #5e3f91;
  }

  .img-thumbnail {
    border: 3px solid #764ba2;
  }
</style>
{% endblock %}

{% block body %}
<div class="container my-5">
  <div class="card">
    <div class="profile-header">
      <img src="{{ asset(app.user.photoProfil ? 'uploads/' ~ app.user.photoProfil : 'images/default-avatar.png') }}" alt="Photo de profil">
      <h3>{{ app.user.nom }} {{ app.user.prenom }}</h3>
    </div>

    <div class="card-body">
      <ul class="nav nav-tabs mb-4" id="profileTab" role="tablist">
        <li class="nav-item" role="presentation">
          <button class="nav-link active" id="personal-tab" data-bs-toggle="tab" data-bs-target="#personal" type="button" role="tab">✅ Informations personnelles</button>
        </li>
        <li class="nav-item" role="presentation">
          <button class="nav-link" id="privacy-tab" data-bs-toggle="tab" data-bs-target="#privacy" type="button" role="tab">🔐 Confidentialité</button>
        </li>
        <li class="nav-item" role="presentation">
          <button class="nav-link" id="prefs-tab" data-bs-toggle="tab" data-bs-target="#prefs" type="button" role="tab">⚙️ Préférences</button>
        </li>
      </ul>

      {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
      {% endfor %}
      {% for message in app.flashes('error') %}
        <div class="alert alert-danger">{{ message }}</div>
      {% endfor %}

      {{ form_start(form, {
        'attr': {
          'enctype': 'multipart/form-data',
          'id': 'editForm',
          'novalidate': 'novalidate'
        }
      }) }}
<input type="hidden" name="action" id="actionInput" value="">
      <div class="tab-content" id="profileTabContent">
        <div class="tab-pane fade show active" id="personal" role="tabpanel">
          <div class="mb-3">
            {{ form_label(form.nom) }}
            {{ form_widget(form.nom, { 'attr': {'class': 'form-control', 'id': form.nom.vars.id} }) }}
            <div class="error-msg" id="nomError"></div>
          </div>

          <div class="mb-3">
            {{ form_label(form.prenom) }}
            {{ form_widget(form.prenom, { 'attr': {'class': 'form-control', 'id': form.prenom.vars.id} }) }}
            <div class="error-msg" id="prenomError"></div>
          </div>

          <div class="mb-3">
            {{ form_label(form.email) }}
            {{ form_widget(form.email, { 'attr': {'class': 'form-control', 'id': form.email.vars.id} }) }}
            <div class="error-msg" id="emailError"></div>
          </div>

          <div class="mb-3">
            {{ form_label(form.numero_telephone) }}
            {{ form_widget(form.numero_telephone, { 'attr': {'class': 'form-control', 'id': form.numero_telephone.vars.id} }) }}
            <div class="error-msg" id="telephoneError"></div>
          </div>
        </div>

       <div class="tab-pane fade" id="privacy" role="tabpanel">
  <p>🔒 Ici tu peux gérer ta confidentialité.</p>

  <!-- Bouton déclencheur -->
  <div class="text-end mt-4">
  <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#confirmModal">
    Désactiver le compte
  </button>
</div>


</div>


        <div class="tab-pane fade" id="prefs" role="tabpanel">
          <div class="mb-3 text-center">
            <label class="form-label">Prévisualisation :</label><br>
            <img id="preview-image" src="{{ asset(app.user.photoProfil ? 'uploads/' ~ app.user.photoProfil : 'images/default-avatar.png') }}" alt="Prévisualisation" class="img-thumbnail" style="width: 120px; height: 120px; object-fit: cover;">
          </div>

          <div class="mb-3">
            {{ form_label(form.photo_profil) }}
            {{ form_widget(form.photo_profil, {
              'attr': {
                'class': 'form-control',
                'id': 'photoUpload',
                'accept': 'image/*'
              }
            }) }}
          </div>
          <a href="{{ path('app_face_id_register') }}" class="btn btn-outline-primary mt-3">
    Enregistrer mon visage 🧠📷
</a>

        </div>
      </div>

      <div class="text-end mt-4">
        <button class="btn btn-primary">Enregistrer les modifications</button>
      </div>


      {{ form_end(form) }}
    </div>
  </div>
</div>
<!-- Modal de confirmation -->
<!-- Modal de confirmation -->
<div class="modal fade" id="confirmModal" tabindex="-1" aria-labelledby="confirmModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title text-danger" id="confirmModalLabel">⚠️ Désactivation</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
      </div>
      <div class="modal-body">
        Êtes-vous sûr de vouloir désactiver votre compte ?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
        <button type="button" class="btn btn-danger" onclick="submitDeactivate()">Oui, désactiver</button>
      </div>
    </div>
  </div>
</div>

{% endblock %}

{% block javascripts %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script>
  document.addEventListener('DOMContentLoaded', function () {
    const fileInput = document.getElementById('photoUpload');
    const previewImage = document.getElementById('preview-image');

    if (fileInput && previewImage) {
      fileInput.addEventListener('change', () => {
        const file = fileInput.files[0];
        if (file && file.type.startsWith('image/')) {
          const reader = new FileReader();
          reader.onload = e => previewImage.src = e.target.result;
          reader.readAsDataURL(file);
        }
      });
    }

    const form = document.getElementById('editForm');
    form.addEventListener('submit', function (e) {
      let valid = true;

      const nom = document.getElementById('{{ form.nom.vars.id }}');
      const prenom = document.getElementById('{{ form.prenom.vars.id }}');
      const email = document.getElementById('{{ form.email.vars.id }}');
      const tel = document.getElementById('{{ form.numero_telephone.vars.id }}');

      document.querySelectorAll('.error-msg').forEach(el => el.innerText = '');

      const emailRegex = /^[^@\s]+@[^@\s]+\.[^@\s]+$/;
      const telRegex = /^[0-9]{8,15}$/;

      if (!nom.value.trim()) {
        document.getElementById('nomError').innerText = 'Le nom est requis.';
        valid = false;
      }
      if (!prenom.value.trim()) {
        document.getElementById('prenomError').innerText = 'Le prénom est requis.';
        valid = false;
      }
      if (!email.value.trim() || !emailRegex.test(email.value.trim())) {
        document.getElementById('emailError').innerText = 'Adresse email invalide.';
        valid = false;
      }
      if (!tel.value.trim() || !telRegex.test(tel.value.trim())) {
        document.getElementById('telephoneError').innerText = 'Numéro de téléphone invalide.';
        valid = false;
      }

      if (!valid) {
        e.preventDefault();
      }
    });
  });
 function submitDeactivate() {
    document.getElementById('actionInput').value = 'disable';
    document.getElementById('editForm').submit();
  }
</script>
{% endblock %}
